---
---

Lambda e2e tests now import the live workspace modules (lambda / javascript / type) by relative path with their controllers staged at their current versions, instead of pinning published registry versions. Test-infra only — no published artifact changes.
