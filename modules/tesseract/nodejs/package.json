{
  "name": "@telorun/tesseract-build",
  "version": "0.1.0",
  "private": true,
  "description": "Build-only package for the std/tesseract module's bundled controller. Never published — the controller ships inside the module artifact, and the Tesseract engine and its models in its assets layer. This package exists to declare the dependencies esbuild inlines and to TYPE-CHECK the source, which esbuild does not do.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.lib.json",
    "test": "vitest run"
  },
  "dependencies": {
    "wasm-feature-detect": "1.9.0"
  },
  "devDependencies": {
    "@telorun/sdk": "workspace:*",
    "@types/node": "^20.0.0",
    "esbuild": "catalog:",
    "typescript": "^5.0.0",
    "vitest": "^2.1.8"
  }
}
