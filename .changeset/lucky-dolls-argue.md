---
"@telorun/sql-sqlite": patch
---

Package description names the kind by its current prefix, `SQLite.Connection`.

The module's `metadata.name` moved from `sql-sqlite` to `SQLite`, so the kind it contributes is `SQLite.Connection`. The npm description still said `SqlSqlite.Connection` — a name nothing resolves any more. Description text only; no code change.
