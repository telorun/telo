{
  "name": "@telorun/record-stream",
  "version": "0.17.0",
  "private": true,
  "description": "Telo record-stream module — stream operations on structured records, and the RecordStream.JournalStore contract plus the journal protocol written once above it. Never published: controllers ship inside the module artifact, and dependent modules reach this code through the module's exports.code entry.",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "source": "./src/index.ts",
      "types": "./dist/index.d.ts",
      "bun": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.lib.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@telorun/sdk": "workspace:*",
    "@types/node": "^20.0.0",
    "esbuild": "catalog:",
    "typescript": "^5.0.0",
    "vitest": "^2.1.8"
  },
  "peerDependencies": {
    "@telorun/sdk": "*"
  }
}
