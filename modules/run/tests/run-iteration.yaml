kind: Telo.Application
metadata:
  name: RunIteration
  version: 1.0.0
imports:
  Run: ../
  Assert: ../../assert
targets:
  - !ref notifyEach
  - !ref expectEvents
---
kind: Run.Iteration
metadata:
  name: notifyEach
collection: !cel "['a', 'b', 'c']"
steps:
  - name: notify
    inputs:
      who: !cel "item"
      # `index + 1`, not a bare `index`: the declared type is `integer`, so a
      # CEL int64, and only arithmetic against an int literal exercises the
      # overload that used to fail at dispatch on a JS number.
      idx: !cel "index + 1"
    invoke:
      kind: Run.Value
      inputs:
        who: {}
        idx: {}
      value:
        notified: !cel "inputs.who"
        at: !cel "inputs.idx"
---
kind: Assert.Events
metadata:
  name: expectEvents
filter:
  - type: "*"
expect:
  - event: "*.Invoked"
    payload:
      ref: { kind: Run.Value }
      outputs: { notified: a, at: 1 }
  - event: "*.Invoked"
    payload:
      ref: { kind: Run.Value }
      outputs: { notified: b, at: 2 }
  - event: "*.Invoked"
    payload:
      ref: { kind: Run.Value }
      outputs: { notified: c, at: 3 }
