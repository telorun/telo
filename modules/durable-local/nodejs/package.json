{
  "name": "@telorun/durable-local-build",
  "version": "0.3.0",
  "private": true,
  "description": "Build-only package for the std/durable-local module's bundled controllers. Never published — the controllers ship inside the module artifact as pkg:telo/local/js bundles. This package exists to declare the dependencies esbuild inlines and to TYPE-CHECK the source, which esbuild does not do.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.lib.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@telorun/sdk": "workspace:*",
    "@types/node": "^20.0.0",
    "esbuild": "catalog:",
    "typescript": "^5.0.0",
    "vitest": "^2.1.8"
  }
}
