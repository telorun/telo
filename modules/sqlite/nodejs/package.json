{
  "name": "@telorun/sqlite-build",
  "version": "0.7.0",
  "private": true,
  "description": "Build-only package for the std/sqlite module's bundled controller. Never published — the controller ships inside the module artifact and better-sqlite3's addon in its native layers. This package exists to declare the dependencies esbuild inlines and to TYPE-CHECK the source, which esbuild does not do.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.lib.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@telorun/sql": "workspace:*",
    "better-sqlite3": "12.8.0",
    "kysely": "^0.28.15"
  },
  "devDependencies": {
    "@telorun/sdk": "workspace:*",
    "@types/better-sqlite3": "^7.0.0",
    "@types/bun": "^1.3.10",
    "@types/node": "^20.0.0",
    "esbuild": "catalog:",
    "typescript": "^5.0.0",
    "vitest": "^2.1.8",
    "yaml": "^2.8.3"
  }
}
