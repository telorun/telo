{
  "name": "@telorun/durable-values-fixture-build",
  "version": "0.0.0",
  "private": true,
  "description": "Build-only package for the durable-values TEST FIXTURE's controller. Never published, and not a module of the standard library — it exists to produce CEL values a manifest has no literal for.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.lib.json"
  },
  "devDependencies": {
    "@telorun/sdk": "workspace:*",
    "@types/node": "^20.0.0",
    "esbuild": "catalog:",
    "typescript": "^5.0.0"
  }
}
