---
---

Documentation-only: each module's docs now state which `Telo.Import` alias their kind references assume (e.g. `Ai.Model` assumes alias `Ai`), so readers know to substitute their own alias. Docs live under `modules/<name>/docs/` and are not part of any published npm package, so no version bump is needed.
