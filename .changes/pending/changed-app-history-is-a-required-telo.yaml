modules:
  blueprints/agent-app: Added
body: "Breaking: App `history` is a required Telo.HostPath with no default: pass it from the
  application's own variable declared x-telo-type: Telo.HostPath (for example default
  `.telo/conversations.sqlite`), which resolves it against the working directory."
