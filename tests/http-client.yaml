kind: Telo.Application
metadata:
  name: HttpClientTest
  version: 1.0.0
targets:
  - TestGetAbsoluteUrl
  - TestGetWithClient
  - TestPostWithJsonBody
  - TestNotFoundDoesNotThrow
---
kind: Telo.Import
metadata:
  name: Http
source: ../modules/http-client
---
kind: Telo.Import
metadata:
  name: Run
source: ../modules/run
---
kind: Telo.Import
metadata:
  name: Assert
source: ../modules/assert
---
# Shared Http.Client with baseUrl for tests that exercise client resolution
kind: Http.Client
metadata:
  name: PlaceholderApi
baseUrl: https://jsonplaceholder.typicode.com
timeout: 10000
---
# Test 1: GET with absolute URL — verifies status + JSON body deserialization
kind: Run.Sequence
metadata:
  name: TestGetAbsoluteUrl
steps:
  - name: FetchTodo
    inputs: {}
    invoke:
      kind: Http.Request
      url: https://jsonplaceholder.typicode.com/todos/1
      method: GET

  - name: AssertGetResponse
    inputs:
      status: "${{ steps.FetchTodo.result.status }}"
      id: "${{ steps.FetchTodo.result.body.id }}"
    invoke:
      kind: Assert.Schema
      schema:
        type: object
        properties:
          status:
            type: integer
            const: 200
          id:
            type: integer
            const: 1
        required:
          - status
          - id
---
# Test 2: GET with Http.Client (relative URL) — verifies baseUrl merging
kind: Run.Sequence
metadata:
  name: TestGetWithClient
steps:
  - name: FetchUser
    inputs: {}
    invoke:
      kind: Http.Request
      client: PlaceholderApi
      url: /users/1
      method: GET

  - name: AssertClientResponse
    inputs:
      status: "${{ steps.FetchUser.result.status }}"
      id: "${{ steps.FetchUser.result.body.id }}"
    invoke:
      kind: Assert.Schema
      schema:
        type: object
        properties:
          status:
            type: integer
            const: 200
          id:
            type: integer
            const: 1
        required:
          - status
          - id
---
# Test 3: POST with JSON body — verifies serialization + 201 response
kind: Run.Sequence
metadata:
  name: TestPostWithJsonBody
steps:
  - name: CreatePost
    inputs: {}
    invoke:
      kind: Http.Request
      url: https://jsonplaceholder.typicode.com/posts
      method: POST
      body:
        title: Hello
        body: World
        userId: 1

  - name: AssertPostResponse
    inputs:
      status: "${{ steps.CreatePost.result.status }}"
      title: "${{ steps.CreatePost.result.body.title }}"
    invoke:
      kind: Assert.Schema
      schema:
        type: object
        properties:
          status:
            type: integer
            const: 201
          title:
            type: string
            const: Hello
        required:
          - status
          - title
---
# Test 4: 4xx response must NOT throw — returns response object per spec section 3.1
kind: Run.Sequence
metadata:
  name: TestNotFoundDoesNotThrow
steps:
  - name: FetchMissing
    inputs: {}
    invoke:
      kind: Http.Request
      url: https://jsonplaceholder.typicode.com/todos/999999
      method: GET

  - name: AssertNotFoundGraceful
    inputs:
      status: "${{ steps.FetchMissing.result.status }}"
    invoke:
      kind: Assert.Schema
      schema:
        type: object
        properties:
          status:
            type: integer
            const: 404
        required:
          - status
---
kind: Assert.Events
metadata:
  name: VerifyAllInvocations
filter:
  - type: "*"
expect:
  - event: Http.Request.*.Invoked
    payload:
      outputs:
        status: 200
  - event: Http.Request.*.Invoked
    payload:
      outputs:
        status: 200
  - event: Http.Request.*.Invoked
    payload:
      outputs:
        status: 201
  - event: Http.Request.*.Invoked
    payload:
      outputs:
        status: 404
