modules:
  modules/fs: Fixed
body: "Writing content that is neither text nor bytes no longer claims bytes cannot be
  written inline in a manifest: the refusal names the ways raw bytes arrive - a
  producing resource or a file embedded with !include-bytes - and encoding: base64 for
  binary spelled out as text."
