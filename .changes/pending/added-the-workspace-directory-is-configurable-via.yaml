modules:
  apps/authoring-agent: Added
body: The workspace directory is configurable via the WORKSPACE_DIR environment variable
  (default ./workspace). A runner standing this up as a session's co-resident agent
  points it at the shared workspace volume, so the agent's file tools and spawned
  commands write the same tree the application containers watch.
