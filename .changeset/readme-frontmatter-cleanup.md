---
---

Strip Docusaurus-only `description:` frontmatter from package READMEs and tighten the leading paragraphs so the first prose under the H1 actually describes the package on GitHub. Documentation-only — no published-package behavior change.
