kind: Telo.Library
metadata:
  name: UncoveredCodeFixture
  version: 1.0.0
imports:
  Http: ../..
  Run: ../../../run
---
# Handler resolves to union {A, B} via passthrough in both branches. The route
# covers A but neither B nor a catch-all → UNCOVERED_THROW_CODE on B.
kind: Run.Sequence
metadata:
  name: throwsAOrB
steps:
  - name: branch
    if: "${{ true }}"
    then:
      - name: throwA
        throw:
          code: "A"
          message: "throws A"
    else:
      - name: throwB
        throw:
          code: "B"
          message: "throws B"
---
kind: Http.Api
metadata:
  name: uncoveredApi
routes:
  - request:
      path: /x
      method: GET
    handler: !ref throwsAOrB
    returns:
      - status: 200
        content:
          application/json:
            body: { ok: true }
    catches:
      - when: "${{ error.code == 'A' }}"
        status: 400
        content:
          application/json:
            body: { error: "${{ error.message }}" }
