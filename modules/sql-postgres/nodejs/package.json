{
  "name": "@telorun/sql-postgres-build",
  "version": "0.0.3",
  "private": true,
  "description": "Build-only package for the std/sql-postgres module's bundled controller. Never published — the controller ships inside the module artifact as a pkg:telo/local/js bundle. This package exists to declare the dependencies esbuild inlines and to TYPE-CHECK the source, which esbuild does not do.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.lib.json && esbuild src/connection-controller.ts --bundle --format=esm --platform=node --target=node20 --external:@telorun/sdk --conditions=source --outdir=. --banner:js='import { createRequire as __teloCreateRequire } from \"node:module\";const require = __teloCreateRequire(import.meta.url);' --out-extension:.js=.mjs"
  },
  "dependencies": {
    "@telorun/sql": "workspace:*",
    "pg": "^8.20.0",
    "kysely": "^0.28.15"
  },
  "devDependencies": {
    "@telorun/sdk": "workspace:*",
    "@types/node": "^20.0.0",
    "@types/pg": "^8.0.0",
    "esbuild": "catalog:",
    "typescript": "^5.0.0"
  }
}
