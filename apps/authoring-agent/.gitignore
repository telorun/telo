# Agent-generated files land in the workspace; keep only the placeholder.
workspace/*
!workspace/.gitkeep

# The agent's state before AGENT_STATE_DIR existed; left in place, never read.
tmp/

# Workspaces the chat tests boot the agent on.
chat/tests/.scratch/
