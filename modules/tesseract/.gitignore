# Staged by `telo release stage` from the `sources:` block, and by the kernel on
# first use in a checkout: the engine builds, the bundled models, and the
# engine's upstream notice. `notices/tessdata.LICENSE` is checked in.
/engine/
/tessdata/
/notices/tesseract.js-core.LICENSE
