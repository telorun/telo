# Staged by `telo release stage` from the `sources:` block, and by the kernel on
# first use in a checkout: Skia's addon for this host, and pdf.js's fonts, CMaps,
# wasm decoders and worker.
/native/
/assets/
/notices/
