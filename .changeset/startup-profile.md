---
"@telorun/cli": minor
"@telorun/kernel": minor
---

Add `telo run --startup-profile <file>` to write a low-overhead JSON breakdown of CLI-visible startup phases and per-resource create/initialization timings.