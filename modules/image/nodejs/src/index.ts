/**
 * Image primitives — `Image.Overlay` draws labelled rectangles onto an image
 * (`@napi-rs/canvas`), the visualization half of vision-grounding loops:
 * render, let a model propose boxes, draw them, look again. Coordinates are
 * pixels, top-left origin.
 */
