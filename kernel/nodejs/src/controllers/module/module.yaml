type: "object"
properties:
  metadata:
    type: "object"
    properties:
      name:
        type: "string"
    required: ["name"]
  source:
    type: "string"
    description: "Path to module directory (relative to runtime root)"
required: ["metadata", "source"]
