## 0.5.0 - 2026-06-07
### Added
* Module `description` and schema `examples:` for registry / MCP discovery (`search_modules` + `get_module_manifest`).
* Bucket reference slots use the unified `!ref` form; the legacy inline `{kind, name}` / string ref shapes are no longer advertised in the schema.