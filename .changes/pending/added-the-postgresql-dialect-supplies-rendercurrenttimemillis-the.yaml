modules:
  modules/postgres: Added
body: The PostgreSQL dialect supplies renderCurrentTimeMillis(), the database clock in
  epoch milliseconds (clock_timestamp-based, so it is read at the statement rather than
  at the transaction's start).
