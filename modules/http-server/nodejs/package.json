{
  "name": "@telorun/http-server-build",
  "version": "0.32.0",
  "private": true,
  "description": "Build-only package for the std/http-server module's bundled controller. Never published — the controller ships inside the module artifact and Scalar's browser bundle in its assets layer. This package exists to declare the dependencies esbuild inlines and to TYPE-CHECK the source, which esbuild does not do.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.lib.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@fastify/static": "^8.0.0",
    "@fastify/swagger": "^9.6.1",
    "@scalar/core": "0.3.36",
    "@scalar/openapi-parser": "0.24.6",
    "@sinclair/typebox": "^0.34.48",
    "@telorun/http-dispatch": "workspace:*",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "fastify": "^5.12.1"
  },
  "devDependencies": {
    "@scalar/fastify-api-reference": "1.44.6",
    "@telorun/sdk": "workspace:*",
    "@types/node": "^20.0.0",
    "esbuild": "catalog:",
    "typescript": "^5.0.0",
    "vitest": "^2.1.8",
    "yaml": "^2.8.3"
  }
}
