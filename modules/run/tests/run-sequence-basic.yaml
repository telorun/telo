kind: Telo.Application
metadata:
  name: RunSequenceBasic
  version: 1.0.0
imports:
  Run: ../
  Assert: ../../assert
targets:
  - !ref testInvokeStep
---
kind: Run.Sequence
metadata:
  name: testInvokeStep
steps:
  - name: addNumbers
    inputs:
      a: 5
      b: 3
    invoke:
      kind: Run.Value
      inputs:
        a: { type: number }
        b: { type: number }
      value:
        sum: !cel "inputs.a + inputs.b"
  - name: verifySum
    inputs:
      sum: "${{ steps.addNumbers.result.sum }}"
    invoke:
      kind: Assert.Schema
      schema:
        type: object
        properties:
          sum:
            type: number
            const: 8
---
kind: Assert.Events
metadata:
  name: expectEvents
filter:
  - type: "*"
expect:
  - event: "*.Invoked"
    payload:
      ref: { kind: Run.Value }
      outputs:
        sum: 8
  - event: "*.Invoked"
    payload:
      ref: { kind: Assert.Schema }
      outputs: true
