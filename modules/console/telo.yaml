kind: Kernel.Library
metadata:
  name: console
  namespace: std
  version: 1.1.0
---
kind: Kernel.Definition
metadata:
  name: WriteLine
capability: Kernel.Runnable
controllers:
  - pkg:npm/@telorun/console@0.1.7?local_path=./nodejs#writeline
schema:
  type: object
  properties:
    output:
      title: Output
      description: Text content written to standard output.
      type: string
  required: ["output"]
---
kind: Kernel.Definition
metadata:
  name: ReadLine
capability: Kernel.Runnable
controllers:
  - pkg:npm/@telorun/console@0.1.7?local_path=./nodejs#readline
schema:
  type: object
  properties:
    prompt:
      title: Prompt
      description: Prompt text shown before reading input.
      type: string
  required: ["prompt"]
