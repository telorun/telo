kind: Telo.Application
metadata:
  name: AgentConsole
  categories: [ AI ]
  description: |
    Interactive REPL backed by an Ai.Agent with tools. Each turn, the agent may
    call tools before answering:
      - a regular HTTP tool (`get_weather`) that fetches current weather from the
        open-meteo API via Http.Request, and
      - every tool exposed by the Telo registry MCP server at
        https://registry.telo.run/mcp, discovered at runtime via AiMcp.ToolProvider.
    The conversation is persisted in sqlite so each turn sees prior history and the
    chat survives restart. Type `/exit` (or send EOF) to leave.

    Run with the OpenAI key in a sibling `.env.local` file:
        OPENAI_API_KEY=sk-...
  version: 1.0.0
imports:
  AiOpenai: oci://ghcr.io/telorun/ai-openai@0.16.0#sha256-DGAJ0wG1FdNAj7XWWrpgOuJgh07fEGg4ymvB6ODiEhY
  Ai: oci://ghcr.io/telorun/ai@0.17.0#sha256-0u0_iSMlz0HmJc0VpEEINgJvErfBEudvvlXBJ76FSmc
  AiMcp: oci://ghcr.io/telorun/ai-mcp@0.12.0#sha256-C0pozBwpE2xzqEsxCJ5YbpqdiHKpxw90Wvn7Md2Ynus
  Mcp: oci://ghcr.io/telorun/mcp-client@0.15.0#sha256-M3frsOwjZWR_Vo2LERsRjBqcN7iXZbVg_gTjK4OGq8s
  Http: oci://ghcr.io/telorun/http-client@0.21.2#sha256-9n-zEKDe8J6T_pfQ0XCcjtUt_dVW5G3APow5BQlmZWE
  Console: oci://ghcr.io/telorun/console@0.17.4#sha256-T3hXxYNK-UClSE_wssnkYI-urC-4c8CEx82rSBc-dR4
  Run: oci://ghcr.io/telorun/run@0.26.0#sha256-ZhW56yiMCImqBh0wJ4mVaUvh5WuUVIaWErASFx--wCY
  Sql: oci://ghcr.io/telorun/sql@0.23.0#sha256-7OoJzJcGyUpfWe4u1c_4aBXB_isG21LaeN8sPmwfufI
  SQLite: oci://ghcr.io/telorun/sqlite@0.3.0#sha256-l33FRF4pw2ohB7HEnRd1NcX402er1kBS_YnC5W-Bg60
targets:
  - !ref initSchema
  - !ref chatLoop
secrets:
  openaiApiKey:
    env: OPENAI_API_KEY
    type: string
---
# --- Persistence -----------------------------------------------------------
kind: SQLite.Connection
metadata: { name: chatDb }
file: ./tmp/agent-history.sqlite

---
kind: SQLite.Schema
metadata: { name: initSchema }
connection: !ref chatDb
# Imperative, not a declared `Table`: the role domain is a CHECK constraint,
# which the column vocabulary cannot state — declaring the table would drop it.
migrations:
  20260601_create_messages:
    statement: |
      CREATE TABLE IF NOT EXISTS messages (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        role TEXT NOT NULL CHECK (role IN ('system', 'user', 'assistant')),
        content TEXT NOT NULL,
        created_at TEXT NOT NULL DEFAULT (datetime('now'))
      )
---
# --- AI provider -----------------------------------------------------------
kind: AiOpenai.OpenaiModel
metadata: { name: gpt4oMini }
model: gpt-4o-mini
apiKey: !cel "secrets.openaiApiKey"
---
# --- Tool: MCP server (runtime discovery) ----------------------------------
# Streamable-HTTP MCP client pointed at the Telo registry server. AiMcp.ToolProvider
# turns it into an Ai.ToolProvider; the agent never learns it is MCP.
kind: Mcp.HttpClient
metadata: { name: registryMcp }
url: https://registry.telo.run/mcp
clientInfo: { name: agent-console, version: 1.0.0 }
---
kind: AiMcp.ToolProvider
metadata: { name: registryTools }
client: !ref registryMcp
---
# --- Tool: a regular invocable (HTTP weather lookup) -----------------------
kind: Http.Client
metadata: { name: openMeteo }
baseUrl: https://api.open-meteo.com
---
# Static request config; the latitude/longitude/query are supplied per call by the
# Ai.Tools `inputs:` mapping below.
kind: Http.Request
metadata: { name: getWeather }
client: !ref openMeteo
throwOnHttpError: true
---
# Wrap the HTTP request as a tool. The model produces { latitude, longitude }; the
# `inputs:` mapping shapes them into the open-meteo request.
kind: Ai.Tools
metadata: { name: weatherTools }
tools:
  - tool: !ref getWeather
    name: get_weather
    description: Get the current weather at a latitude/longitude (open-meteo).
    parameters:
      type: object
      additionalProperties: false
      required: [ latitude, longitude ]
      properties:
        latitude: { type: number, description: "Latitude in decimal degrees." }
        longitude: { type: number, description: "Longitude in decimal degrees." }
    inputs:
      url: /v1/forecast
      method: GET
      query:
        latitude: !cel "arguments.latitude"
        longitude: !cel "arguments.longitude"
        current_weather: "true"
    result: !cel "result.body"
---
# --- Agent -----------------------------------------------------------------
kind: Ai.Agent
metadata: { name: assistant }
model: !ref gpt4oMini
system: |
  You are a helpful CLI assistant. Use the available tools when they help answer;
  don't guess at facts you can look up. Keep replies brief.
maxSteps: 6
toolProviders:
  - provider: !ref weatherTools
  - provider: !ref registryTools
    prefix: "registry_"
---
# --- REPL ------------------------------------------------------------------
kind: Run.Sequence
metadata: { name: chatLoop }
steps:
  - name: read
    invoke: !ref Console.readLine
    inputs:
      prompt: "{cyan.bold you} › "
  - name: converse
    while: !cel "steps.read.result.value != '/exit'"
    do:
      - name: insertUser
        inputs:
          sql: "INSERT INTO messages (role, content) VALUES (?, ?)"
          bindings: [ "user", "${{ steps.read.result.value }}" ]
        invoke:
          kind: Sql.Command
          connection: !ref chatDb
      - name: readHistory
        inputs:
          sql: "SELECT role, content FROM messages ORDER BY id ASC"
        invoke:
          kind: Sql.Query
          connection: !ref chatDb
      - name: ask
        invoke: !ref assistant
        inputs:
          messages: !cel "steps.readHistory.result.rows"
      - name: showTools
        if: !cel "steps.ask.result.steps.exists(s, s.toolCalls.size() > 0)"
        then:
          - name: printTools
            invoke: !ref Console.writeLine
            inputs:
              output: >-
                {dim 🔧 ${{ steps.ask.result.steps
                  .map(s, s.toolCalls.map(tc, tc.name).join(', '))
                  .filter(line, line != '')
                  .join(' → ') }}}
      - name: print
        invoke: !ref Console.writeLine
        inputs:
          output: "{magenta.bold ai}  › ${{ steps.ask.result.text }}"
      - name: insertAssistant
        inputs:
          sql: "INSERT INTO messages (role, content) VALUES (?, ?)"
          bindings: [ "assistant", "${{ steps.ask.result.text }}" ]
        invoke:
          kind: Sql.Command
          connection: !ref chatDb
      - name: read
        invoke: !ref Console.readLine
        inputs:
          prompt: "{cyan.bold you} › "
