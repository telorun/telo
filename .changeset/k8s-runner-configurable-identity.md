---
"@telorun/k8s-runner": minor
---

Advertised runner identity is operator-configurable via `RUNNER_DISPLAY_NAME` / `RUNNER_DESCRIPTION` (chart: `runner.displayName` / `runner.description`), defaulting to "Telo Runner" / "Runs the Telo application in a cloud environment"
