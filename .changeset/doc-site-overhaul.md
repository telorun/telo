---
---

Documentation-site reshuffle: introduce a separate `@docusaurus/plugin-content-docs` instance for the standard library (URL moves from `/modules/` to `/standard-library/`), promote Getting Started to a top-level sidebar entry, move generic guides to `docs/guides/`, add `docs/coding-agents.md` covering the MCP server, add a "File layout" + env-var section to the registry MCP `instructions`, fix several cross-doc and image links, and rework the llms-txt plugin to serve `/llms.txt`, `/llms-full.txt`, and per-page `.md` companions in dev as well as build. No published-package surface area changes.
