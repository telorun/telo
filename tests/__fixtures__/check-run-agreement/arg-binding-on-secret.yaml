# A secret on the command line is readable in the process table.
kind: Telo.Application
metadata:
  name: ArgBindingOnSecret
secrets:
  token:
    type: string
    env: TOKEN
    arg: token
    default: t
