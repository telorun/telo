*
!cli/nodejs
!kernel/nodejs
!analyzer/nodejs
!analyzer/migrations
!analyzer/artifact-axes
!templating/nodejs
!sdk/nodejs
!sdk/value-types
!sdk/zone-attributes
!scripts
!packages/ide-support
!packages/debug-wire
# `telo runner` serves the /v1 session contract from this package, so the CLI
# does not build without it.
!packages/runner-core
!packages/debug-ui
!packages/glob/nodejs
!package.json
!pnpm-*
!tsconfig.base.json

Dockerfile*
node_modules
dist
