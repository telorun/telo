# Imports the library next door and does nothing else. Its entry-scoped check
# is silent about the library's kinds by design, so what refuses them here is
# the kernel registering each one.
kind: Telo.Application
metadata:
  name: CallableKindsApp
  version: 1.0.0
imports:
  Lib: ./lib
