modules:
  modules/assert: Added
body: "Assert.Manifest accepts `expect.stdout`: it runs the manifest, requires exit 0,
  and compares everything the run wrote to stdout with the given string exactly. The run
  gets its own stdout, so the assertion behaves the same on a developer machine, in CI
  and inside a container, without starting `telo` as a shell command."
