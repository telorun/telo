{{- if .Values.networkPolicy.enabled }}
# Session-pod network policy. HONEST SCOPE: this is NOT default-deny egress.
# Every session resolves its own module closure in-pod (`telo run` fetches
# modules), so session pods need to reach the package registries. This policy
# therefore allows egress to the public internet EXCEPT the blocked CIDRs (cloud
# metadata + RFC1918), and isolates pod-to-pod traffic on ingress. It does NOT
# confine exfiltration to a registry allowlist — core NetworkPolicy is CIDR-only
# and cannot express FQDN allows; that requires a CNI with FQDN policy (Cilium)
# or an egress proxy. Enforced only if the cluster CNI supports NetworkPolicy at
# all.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "k8s-runner.name" . }}-sessions
  namespace: {{ .Values.sessionNamespace }}
  labels:
    {{- include "k8s-runner.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/managed-by: telo-k8s-runner
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # The runner reaches session pods; deny pod-to-pod traffic between sessions
    # (no podSelector peer is allowed).
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.runnerNamespace }}
    {{- if and .Values.sessionRouting.baseDomain .Values.sessionRouting.dataPlaneNamespace }}
    # Session traffic enters via the routing DATA PLANE, usually in its own
    # namespace — ingress-nginx, an Envoy Gateway deployment, an Istio gateway.
    # Without this peer its requests are dropped and per-session routing breaks
    # AFTER the route programmed correctly, which is the confusing failure: the
    # routing layer reports success and the workload is still unreachable.
    #
    # Named for the data plane rather than for an ingress controller because the
    # two routing layers put it in different places, and a mesh may put it
    # somewhere else again.
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.sessionRouting.dataPlaneNamespace }}
    {{- end }}
  egress:
    # DNS — scoped to the cluster DNS namespace (default kube-system) rather than
    # every namespace, so port 53 isn't a hole in the cluster-internal block.
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.networkPolicy.dnsNamespace }}
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    # The runner Service (bundle fetch) — explicit carve-out of the
    # cluster-internal block.
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.runnerNamespace }}
    # Everything else EXCEPT the blocked CIDRs (metadata, RFC1918).
    - to:
        - ipBlock:
            cidr: "0.0.0.0/0"
            except:
              {{- range .Values.networkPolicy.blockedCidrs }}
              - {{ . | quote }}
              {{- end }}
{{- end }}
